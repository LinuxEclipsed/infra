apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-pvc
  namespace: actualbudget
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-xfs
  resources:
    requests:
      storage: 2Gi
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyseerr-data-pvc
  namespace: arr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
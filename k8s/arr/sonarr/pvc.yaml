apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-data-nfs-pvc
  namespace: arr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 3Gi